Lesson5 summary***
1)we learn about handling request data
2)Building a web app with basic routes

1)we start of with the similar code as lesson 4 but we had to now
  we had to add some code to analyze the type of incoming requests

2)
--a)using a that converts Javascript objects into a string in order
   to read the type of request method used, req.url, and req.headers

3)we had to handle request data
 --a)first we listen to requests
 --b)make an array to store data content
 --c)we use this callback function to help add request data to the array
 --d)we then convert the body array to string of text then we can
     log the request data to the console...

