chapter 13 summary...

//**Exercise 13.1**//

1)we had to create a function- greet() with an argument of fullname
  we make sure that argument is an array 

2)we create a second function that takes in two arguments a string,
  and a callback function

--a) in this function we split the string argument value into an
    array and store it in a local array

--b)we then take the local array and place it to the callback
   argument(in this case the greet() funct)

3)then we invoke the greetingCall() and give it appropriate 
  arguments...

///******////

///**Projects**//
1-2)we create an array of allowed passwords...
   --made a password checker that returns a boolean value of
      depending on if the password given to it exists in the
       the array

3)in the login function
 
 a-- we make sure that the funct returns a Promise...

 b--using resolve and reject we return a boolean value based on
    if the password is valid or not...

 4)this function checks the password
  
 a--gives the password to the login function

 b--we use then() to output the "Approved" status if the password is
    valid

 c)and we use the catch() to output the "Rejected" status 
   if the password is invalid...

 5)we try out different passwords to see the results...